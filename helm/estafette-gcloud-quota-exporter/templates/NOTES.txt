1. Get the application logs by running this command:
kubectl logs -f -l app.kubernetes.io/name={{ include "estafette-gcloud-quota-exporter.name" . }},app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}